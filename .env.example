# Copy to `.env` and edit. The compose file reads from `.env` automatically.

# ── Required ──────────────────────────────────────────────────────────────────

# Bearer token for every API request. Generate with: openssl rand -hex 32
VAULT_API_TOKEN=replace-me-with-a-real-token

# Absolute host path to your markdown vault. The container mounts this at
# /data and treats it as the source of truth (read-write).
VAULT_DATA_PATH_HOST=/path/to/your/vault-data

# ── Network ───────────────────────────────────────────────────────────────────

# Port the API listens on (host side). Default 8123.
VAULT_PORT=8123

# Host interface to publish on. Default 0.0.0.0 (all interfaces — remote access).
# Use 127.0.0.1 for local-only, or your LAN IP to limit to that subnet.
VAULT_PUBLISH_HOST=0.0.0.0

# URL the homepage.href compose label points at. Set this to whatever you
# reach the dashboard on, e.g. `http://yourhost.lan:8123/ui/`. The default
# falls back to the GitHub repo URL so a fresh checkout has a non-broken
# link until you customize it.
# VAULT_HOMEPAGE_HREF=http://yourhost.lan:8123/ui/

# ── Behavior ──────────────────────────────────────────────────────────────────

# Run a full reindex on container start (default true). Cheap if the DB is
# already in sync; a no-op for unchanged files.
VAULT_AUTO_REINDEX=true

# Watch the vault tree and reindex incrementally on .md changes (default true).
VAULT_AUTO_WATCH=true

# Debounce window in ms before processing a batch of file changes (default 1500).
VAULT_WATCH_DEBOUNCE_MS=1500

# Embedder backend: 'bge' (real, ~150 MB model) or 'fake' (deterministic, no
# semantic value — useful for smoke tests only). Default 'bge'.
VAULT_EMBEDDER=bge

# Idle window (ms) of no embed calls before the BGE pipeline is disposed and
# its ONNX inference arena returned to the OS. Default 1_800_000 (30 min);
# minimum 1000. Reload on first post-idle call adds ~1-3 s; first batch after
# reload reallocates the arena to that batch's shape. Set high (e.g. 86400000
# = 24h) to keep the model effectively resident.
VAULT_EMBEDDER_RETENTION_MS=1800000

# Defensive cap on the batch size handed to a single ORT inference. Attention
# memory scales as batch × heads × seq_len² inside the model; capping bounds
# the active-peak RSS. Default 8 (~200-400 MB peak for BGE-small at S=512);
# minimum 1. Increase for throughput at the cost of memory headroom; decrease
# if the host can't tolerate even the small peak. Tested values: 1 (slowest,
# smallest peak), 8 (default), 32 (faster, ~1 GB peak), 128 (multi-GB peak).
VAULT_EMBEDDER_MAX_BATCH=8

# ── Backup (Tier 1, optional) ─────────────────────────────────────────────────

# When true, a polling loop runs `git add -A && git commit` against the vault
# tree on a schedule so working-tree changes don't sit uncommitted forever
# (default true). No-op when the path isn't a git repo.
VAULT_AUTO_COMMIT=true

# When true (default false), also `git push` after each commit. Requires the
# remote to accept whatever credentials the container has — set this to true
# only if you've mounted SSH keys into ~/.ssh or set up an HTTPS token in the
# remote URL.
VAULT_AUTO_PUSH=false

# Polling interval for auto-commit, in ms. Default 60000 (one minute).
VAULT_COMMIT_INTERVAL_MS=60000

# Author identity for auto-commits. Passed via `git -c user.name=… -c
# user.email=…` so the container doesn't need a global ~/.gitconfig. Defaults
# below are fine for a private vault repo; override if you want auto-commits
# attributed to a specific person/bot.
VAULT_GIT_AUTHOR_NAME=vault-storage
VAULT_GIT_AUTHOR_EMAIL=vault-storage@localhost

# ── Maintenance scan scheduler (C8.1) ─────────────────────────────────────────

# When true (default), the server periodically runs the bundled maintenance
# scans (find-duplicates, compaction/retention/upgrade candidates — same set
# as POST /maintenance/run-all). A tick only actually scans when the vault's
# content changed since the last completed pass, or when the last pass is
# older than VAULT_SCAN_MAX_QUIET_MS (time-driven scans like retention need a
# periodic refresh even on a quiet vault). Manual POST /maintenance/run-all
# always wins and counts as a pass. Shares VAULT_WORK_HOURS_* with git-sync.
VAULT_SCAN_ENABLED=true

# Eligibility-tick cadence, in ms (min 60000). A skipping tick costs one
# meta-table read. Default 3600000 (hourly).
VAULT_SCAN_INTERVAL_MS=3600000

# Force a pass when the last one is older than this, in ms, regardless of
# content changes. Default 604800000 (7 days). 0 = scan on every in-window tick.
VAULT_SCAN_MAX_QUIET_MS=604800000

# ── File ownership (advanced) ─────────────────────────────────────────────────

# Match these to your host user so volume-mounted writes have sane ownership.
# Run `id -u` and `id -g` to find yours; the compose defaults to 1000:1000.
VAULT_UID=1000
VAULT_GID=1000
